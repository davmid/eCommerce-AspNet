@model shop_view.Models.SmartWatch

<h2>Delete SmartWatch</h2>

<p>Are you sure you want to delete the following smartwatch?</p>
<dl class="dl-horizontal">
    <dt>Brand:</dt>
    <dd>@Model.Brand</dd>
    <dt>Category:</dt>
    <dd>@Model.SWCategory</dd>
    <dt>Price:</dt>
    <dd>@Model.Price</dd>
    <dt>Description:</dt>
    <dd>@Model.Description</dd>
</dl>
<form asp-controller="SmartWatch" asp-action="Delete" method="post">
    <input asp-for="SWID" type="hidden" />
    <button type="submit" class="btn btn-danger">Delete</button>
    <a asp-controller="SmartWatch" asp-action="Index" class="btn btn-default">Cancel</a>
</form>
